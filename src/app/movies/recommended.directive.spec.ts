import { RecommendedDirective } from './recommended.directive';

describe('RecommendedDirective', () => {
  it('should create an instance', () => {
    const directive = new RecommendedDirective();
    expect(directive).toBeTruthy();
  });
});
