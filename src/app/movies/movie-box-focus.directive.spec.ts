import { MovieBoxFocusDirective } from './movie-box-focus.directive';

describe('MovieBoxFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new MovieBoxFocusDirective();
    expect(directive).toBeTruthy();
  });
});
